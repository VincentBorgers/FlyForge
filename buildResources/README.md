# Branding assets

Plaats hier je branding-bestanden voor desktop builds.

## Aanbevolen bestanden
- `icon.ico` (Windows)
- `icon.icns` (macOS)
- `icon.png` (Linux)

Zodra deze bestanden aanwezig zijn, gebruikt `electron-builder` ze automatisch op basis van de ingestelde paden in `package.json`.
